# Compression of a 3D lattice metamaterial

Uniaxial compression of a lattice unit cell. The structure loses stability; at
the bifurcation the solution is perturbed along the lowest eigenmode to trace
the post-buckled branch.

## Run

```bash
mpirun -np $nprocs python run_solver.py
```
