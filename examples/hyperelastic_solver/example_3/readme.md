# In-plane compression of a honeycomb metamaterial

In-plane compression of an extruded honeycomb. Stability is monitored every
Newton step; at the bifurcation the solution is perturbed along the lowest
eigenmode to continue onto the post-buckled branch.

## Run

```bash
mpirun -np $nprocs python run_solver.py
```
